#
# Nimbus
# Proxy GAE
# End to End Test Target Nginx Config
#

events {
  worker_connections   1024;
}

http {
  server {
    # create test route for e2e test
    location /test/e2e {
      return 200;
    }
  }
}
