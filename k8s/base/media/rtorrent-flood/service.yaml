#
# Nimbus
# K8s Deployment: Media Streaming Server
# rTorrent-flood
#

apiVersion: v1
kind: Service
metadata:
  name: flood
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
